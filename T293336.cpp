#include<algorithm>
#include<iostream>
#include<cstring>
#include<cstdio>
#define N (100005)
#define M (1005)
#define INF (0x3f3f3f3f)
#define mod (998244353)
#define ll long long
#define PII pair<int,int>
#define ls(xxx) (xxx)<<1
#define rs(xxx) (xxx)<<1|1
using namespace std;
int t,n,m,q,a[N];
inline void solve_20(){
}
bool l_r=1;
inline void solve_l_r(){
	
}
inline void solve_bf(){
	if()
}
int OPT[N],L[N],R[N],x[N]; 
int main(){
	scanf("%d",&t);
	while(t--){
		scanf("%d %d",&n,&m);
		for(int i=1;i<=n;i++) scanf("%d",&a[i]);
		for(int i=1;i<=m;i++){
			scanf("%d %d %d",&OPT[i],&L[i],&R[i]);
			if(OPT[i]==1) scanf("%d",&X[i]);
			if(L[i]!=1 && R[i]!=n) l_r=0;
			if(l_r){
				solve_l_r();
				continue; 
			}
			solve_bf();
		}
		solve_20();
	} 
    return 0;
}
//F**k CCF!!!!!

